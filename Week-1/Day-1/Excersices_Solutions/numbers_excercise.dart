void main() {
//  Given the double below, assign it
//  to a new variable where its rounded to the
//  nearest integer.
  var someNum = 3.1;

  print(someNum.round());

  // Bonus answer: It basically gets rounded up because the law of rounding
  var bonus = 0.5;
  print(bonus.round());
}
// BONUS QUESTION; In the cases of 0.5, how can you
// ensure its rounded up or down?
